{
  "name": "spacetimedb-node-project",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "spacetime build && spacetime generate --lang typescript --out-dir ../webapp/src/module_bindings && spacetime generate --lang typescript --out-dir ../display/src/renderer/src/module_bindings",
    "publish-local": "spacetime publish -s local canal4-dev --yes"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "spacetimedb": "^2.0.2"
  },
  "devDependencies": {
    "typescript": "~5.6.2"
  }
}