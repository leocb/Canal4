{
  "name": "spacetimedb-node-project",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "spacetime build && spacetime generate --yes --lang typescript --out-dir ../webapp/src/module_bindings && spacetime generate --yes --lang typescript --out-dir ../display/src/module_bindings",
    "build:bundle": "spacetime build",
    "publish-local": "spacetime publish -s local canal4-dev --yes",
    "publish-local-delete": "spacetime publish -s local canal4-dev --yes --delete-data"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "spacetimedb": "^2.0.2"
  },
  "devDependencies": {
    "typescript": "~5.6.2"
  }
}